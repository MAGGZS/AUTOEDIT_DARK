fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.31
sqlalchemy>=2.0.36
aiofiles==23.2.1
websockets==12.0
ffmpeg-python==0.2.0

# Driver do PostgreSQL usado no deploy. Local continua em SQLite, que é nativo
# do Python — instalar aqui não muda nada em quem roda na própria máquina.
psycopg[binary]==3.2.3

# Migrações de schema. Necessário para acrescentar as tabelas de usuários sem
# recriar o banco nem editar tabela na mão.
alembic==1.14.0
